console.log(myVariable);
var myVariable = 1;

/**
 * Biên dịch thành:
 * var myVariable;
 * console.log(myVariable);
 * myVariable = 1;
 */

/**
 * Hoisting (dâng lên): dâng lên cao nhất trong scope của nó
 * {}: scope/code block/code body
 * let/const: NO hoisting
 * var: hoisting, giá trị mặc định sẽ là undefined
 */
